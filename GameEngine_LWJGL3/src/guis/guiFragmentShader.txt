#version 140

in vec2 textureCoords;

out vec4 out_Colour;

uniform sampler2D guiTexture;
uniform float hovered;

void main(void)
{
	vec4 colour = texture(guiTexture, textureCoords);
	
	if(hovered == 1.0)
	{
		out_Colour = colour.rbga;
	}
	else
	{
		out_Colour = colour;
	}
}